import { USER_FACING_MESSAGE_STYLE_PROMPT } from '../../../../prompts/userFacingMessageStylePrompt';

export function renderCatalogForemanFinalSummaryPolicy(): string {
  return `## Итог

Завершение только через \`finish_catalog_turn\`.

- \`summary\` — готовый ответ пользователю, который можно отправить как есть.
- Факты из результатов воркеров переноси без изменений: цены, SKU, id и прочие значения не округляй и не переформулируй.
- Не отдавай голые технические id без поясняющей фразы, если пользователь явно не просил только значение.
- Без JSON/YAML, служебных полей, пошагового лога и внутренней механики.
- Если не вышло, коротко скажи, что не удалось или каких данных не хватило.
- Пиши естественно, кратко, абзацами или короткими списками.

${USER_FACING_MESSAGE_STYLE_PROMPT}`;
}
