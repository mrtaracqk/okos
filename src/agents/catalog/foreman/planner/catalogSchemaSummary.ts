export function renderCatalogSchemaSummary(): string {
  return `## Схема каталога

- **Категория**: дерево \`parent\`; у товара в проекции хранятся только привязки категорий по id.
- **Глобальный атрибут / термин**: общий справочник; термин существует только внутри конкретного атрибута.
- **Товар (parent layer)**: \`simple\` или \`variable\`; у variable на родителе живут \`attributes\`, \`default_attributes\` и список id вариаций. Read/list дают сжатую проекцию.
- **Вариация**: существует только внутри известного \`product_id\`; цена, SKU и набор опций относятся к конкретной строке, а не к родителю.`;
}
